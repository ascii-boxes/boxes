# This config file is valid by itself, but should never be referenced.

BOX designD

sample
    D from config file C (ERROR!)
ends


shapes {
     w ("ERROR", "ERROR", "ERROR")
}

elastic (
	w
)

padding {
    left 1
}

END designD
